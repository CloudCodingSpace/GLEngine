{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "type": "cppdbg",
            "externalConsole": true,
            "program": "${workspaceFolder}/bin/engine.exe",
            "name": "GLEngine Launch",
            "request": "launch"
        }
    ]
}