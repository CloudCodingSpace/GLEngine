{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "TestBench & GLEngine Build",
            "type":"cppbuild",
            "command": "${workspaceFolder}/build.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}